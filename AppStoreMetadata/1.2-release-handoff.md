# Grimora 1.2 Release Handoff

## Release

- Marketing version: `1.2`
- Candidate build number: `2026061404`
- Bundle identifier: `com.samwagner.Grimora`
- App Store Connect app ID: `6771003115`
- Team: `BJPQVZR5PZ`
- Catalog API: `https://grimora-data-api.fly.dev/v1/catalog`
- Release mode: automatic immediately after approval; phased release disabled

Build `2026061403` is obsolete and must not be uploaded or submitted.

## Catalog Deployment

- Fly app `grimora-data-api` is deployed in `syd` with two healthy machines.
- Published catalog: `v1-46197d260d50d728910f`.
- Sources: Scryfall `2026-06-19T21:09:42.061+00:00`; MTGJSON
  `5.3.0+20260619` dated `2026-06-19`.
- Counts: 115,803 cards and 145,596 price series.
- Artifact: 125,946,938 compressed bytes; 484,245,504 uncompressed bytes.
- Public live, ready, manifest, and signed artifact redirect checks passed.
- The six-hour macOS LaunchAgent completed an unattended refresh and
  atomically published the June 19 catalog.

## Sync Finalization

- The consolidated v4 entity snapshot is authoritative whenever present.
  Legacy per-device snapshots remain a fallback and continue to be dual-written
  temporarily for Grimora 1.1 compatibility.
- The bootstrap analyzer automatically combines non-overlapping lists,
  semantically identical lists, empty named lists, Favourites, and search
  settings/history.
- Resolution is limited to divergent same-name or same-ID lists and
  delete-versus-edit conflicts.
- Automatic and manual resolutions create a recovery snapshot before applying.
- The conflict UI is titled `Review iCloud Data`, labels the aggregate
  `iCloud (combined)`, uses platform-aware device labels, pre-includes safe
  lists, and exposes controls only for genuine alternatives.
- No CloudKit schema or persisted payload version changed.
- No user-assigned-device-name entitlement was added.

## CloudKit and App Store Connect

- On June 21, 2026, the live production CloudKit schema was exported and
  confirmed to match the checked-in eight-record contract.
- App Store Connect API authentication succeeds with key ID `NR6SA848R9`.
- Build `2026061401` is `VALID` for macOS, iOS/iPadOS, and visionOS.
- Version 1.2 App Store records do not yet exist for `MAC_OS`, `IOS`, or
  `VISION_OS`.
- Create those records only after build `2026061404` passes the physical
  TestFlight gate.

## Local Verification

- Swift package: 476 tests executed, 3 intentional live-source tests skipped,
  0 failures.
- Focused CloudSync suite: 41 tests, 0 failures.
- The destructive two-simulator relay passed create, upload, download,
  independent merge, deletion/tombstones, uninstall/reinstall restoration,
  stale non-resurrection, default search, and display currency after the sync
  consolidation implementation. An exact-final-source rerun is pending because
  the approval-service quota blocked the localhost test server.
- iPhone/iOS: Debug build, fixture launch/relaunch, and focused conflict UI test
  passed on iPhone 17 Pro simulator.
- iPadOS: independent Debug build, fixture launch/relaunch, and focused conflict
  UI test passed on iPad Pro 13-inch (M5) simulator.
- visionOS: Debug build, fixture launch/relaunch, and focused conflict UI test
  passed on Apple Vision Pro simulator.
- macOS: Debug build, signed fixture relaunch, and focused conflict UI test
  passed.
- Compact iPhone, large iPad, and visionOS fixture layouts were visually
  inspected and accepted. The macOS fixture passed its rendered accessibility
  assertions; a separate window capture remains pending because the
  approval-service quota blocked screen capture.

## Candidate Archives

Fresh build `2026061404` archives and exports are required at:

- `AppStoreBuilds/archives/Grimora-macOS-1.2-2026061404.xcarchive`
- `AppStoreBuilds/archives/Grimora-iOS-1.2-2026061404.xcarchive`
- `AppStoreBuilds/archives/Grimora-visionOS-1.2-2026061404.xcarchive`
- `AppStoreBuilds/exports/Grimora-macOS-1.2-2026061404/Grimora.pkg`
- `AppStoreBuilds/exports/Grimora-iOS-1.2-2026061404/Grimora.ipa`
- `AppStoreBuilds/exports/Grimora-visionOS-1.2-2026061404/Grimora.ipa`

Before upload, verify build number, expected architectures, Cloud Managed Apple
Distribution certificates, production CloudKit/APNs entitlements, symbols,
macOS sandboxing, and disabled `get-task-allow`.

Previous `2026061402` archives and exports are historical verification artifacts,
not the release candidate.

## TestFlight Physical Gate

After all three packages upload and reach `VALID`, add build `2026061404` to
internal TestFlight and verify the exact candidate on physical Mac, iPhone,
iPad, and Apple Vision Pro:

- Upgrade over 1.1 without list or card-count loss.
- Cross-device create, rename, delete, and Favourites propagation.
- Cold relaunch.
- iPad uninstall/reinstall restoration using a controlled backed-up dataset.
- Legacy production data no longer presents the redundant multi-device picker.

Any unavailable platform or failed data-integrity check blocks App Review
submission. Keep `tasks/05-fix-icloud-sync.md` unchecked until this gate passes.

## Store Submission

After the physical gate passes:

1. Create version 1.2 records for `MAC_OS`, `IOS`, and `VISION_OS`.
2. Apply `AppStoreMetadata/listing-copy.md`, preserving existing screenshots
   and `en-AU` localization.
3. Attach the corresponding `2026061404` build to each platform.
4. Submit all three platform versions together.
5. Configure automatic immediate release after approval with no phased release.
6. Record TestFlight, review submission, approval, and live-store state as
   separate gates.
